<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" android:layout_width="match_parent"
    android:layout_height="match_parent" android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin"
    tools:context="com.example.zaken.androidappwn.ChooseBusinessMainActivity"
    android:orientation="vertical"
    android:weightSum="1">

    <LinearLayout
        android:orientation="horizontal"
        android:layout_width="fill_parent"
        android:layout_height="fill_parent"
        android:layout_gravity="center_horizontal"
        android:layout_weight="0.5">

        <Button
            android:layout_width="fill_parent"
            android:layout_height="fill_parent"
            android:text="@string/textOnChooseBusinessButton"
            android:id="@+id/chooseBusinessButton"
            android:onClick="choose_business"
            android:textAppearance="@style/Base.TextAppearance.AppCompat.Large"
            android:textSize="70dp"
            android:textStyle="bold|italic"
            android:background="@drawable/blue_buttons"
            android:textColor="@android:color/white"

            android:singleLine="false" />


    </LinearLayout>

    <LinearLayout
        android:orientation="horizontal"
        android:layout_width="fill_parent"
        android:layout_height="fill_parent"
        android:layout_gravity="center_horizontal"
        android:layout_weight="0.5">

        <Button
            android:layout_width="fill_parent"
            android:layout_height="fill_parent"
            android:text="@string/textOnScanQRButton"
            android:id="@+id/scanQRButton"
            android:textAppearance="@style/Base.TextAppearance.AppCompat.Large"
            android:textSize="35sp"
            android:textStyle="bold|italic"
            android:background="@drawable/qrr"
            android:textColor="@android:color/holo_blue_dark"
            android:layout_marginTop="5dp"
            android:onClick="scan" />
    </LinearLayout>
</LinearLayout>